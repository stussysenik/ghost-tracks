# Local-first credit ledger — per-device runtime state, never committed.
.data/

# Local env/secrets — the Stripe secret and Cerebras key live here.
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
